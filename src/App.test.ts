import { describe, it, expect } from 'vitest'

describe('App basic', () => {
  it('should have correct project name in package.json', () => {
    expect(true).toBe(true)
  })

  it('math', () => {
    expect(1 + 1).toBe(2)
  })
})
